plugins {
    id("io.github.gmazzo.test.aggregation")
    id("base")
}

apply(from: "module-convention.gradle")

dependencies {
    aggregateTestsFrom(project(":app"))
    aggregateTestsFrom(project(":lib"))
    aggregateTestsFrom(project(":utils"))
}
